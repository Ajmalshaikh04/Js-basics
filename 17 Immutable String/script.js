//Immutable String
const immutableString = "String is immutable!"; //this string is not modified
const upperCase = immutableString.toUpperCase(); //copies the string and returns a new string

console.log(immutableString, upperCase);
